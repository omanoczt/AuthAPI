import { ErrorCode } from "../../types/ErrorCodes";

export default {code: ErrorCode.invalid_input, name: 'Invalid input', message: 'Invalid input data, check the bounds of strings or numbers, and the types of each data.'};